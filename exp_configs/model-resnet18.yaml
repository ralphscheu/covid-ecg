model: resnet18
grid_search:
  clf__lr:
    - 0.00005
    - 0.00003
    - 0.00001
  clf__batch_size:
    - 32
    - 64
    - 100

# GridSearch config
early_stopping_max_epochs: 100
early_stopping_patience: 10
num_cv_folds: 5
