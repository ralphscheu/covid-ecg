model: mlp
grid_search:
  clf__module__hidden_size: [32, 64, 100]
  clf__lr: [0.0001, 0.00003, 0.00005]
  clf__batch_size: [64, 100]

# GridSearch config
early_stopping_max_epochs: 100
early_stopping_patience: 10
num_cv_folds: 5